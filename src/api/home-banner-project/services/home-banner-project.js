'use strict';

/**
 * home-banner-project service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::home-banner-project.home-banner-project');
